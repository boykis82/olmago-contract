server.port: 80
server.error.include-message: always

logging:
  level:
    root: INFO
    team.caltech.olmago.contract.domain: INFO
    com:
      querydsl:
        sql: INFO
    org:
      hibernate:
        SQL: INFO
        type: INFO

app:
  customer-service:
    host: customer
    port: 8080